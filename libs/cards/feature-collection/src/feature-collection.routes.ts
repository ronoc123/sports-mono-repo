import { Routes } from '@angular/router';
import { CollectionComponent } from './collection/collection.component';

export const collectionRoutes: Routes = [{ path: '', component: CollectionComponent }];
